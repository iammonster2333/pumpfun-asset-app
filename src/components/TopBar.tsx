import React from "react"; import { useWallet } from "./WalletProvider"; import { useAppStore } from "@/stores/useAppStore"; export const TopBar: React.FC = () => { const { wallet, connect, disconnect } = useWallet(); const { theme, toggleTheme } = useAppStore(); return ( <div className="fixed top-0 left-0 right-0 bg-dark-800/80 backdrop-blur-md border-b border-dark-700 z-40"> <div className="flex items-center justify-between px-4 py-3"> <div className="flex items-center space-x-2"> <div className="w-8 h-8 bg-gradient-to-br from-primary to-success rounded-lg flex items-center justify-center"> <span className="text-white font-bold text-sm">P</span> </div> <span className="text-lg font-bold gradient-text">PumpFun</span> </div> <div className="flex items-center space-x-3"> <button onClick={toggleTheme} className="w-8 h-8 bg-dark-700 rounded-lg flex items-center justify-center text-gray-300 hover:text-white transition-colors"> {theme === "dark" ? "" : ""} </button> {wallet.connected ? ( <div className="flex items-center space-x-2"> <div className="bg-success/20 text-success px-3 py-1 rounded-full text-sm"> <span className="w-2 h-2 bg-success rounded-full inline-block mr-2"></span> 已连接 </div> <button onClick={disconnect} className="btn btn-sm btn-ghost text-gray-300 hover:text-white"> 断开 </button> </div> ) : ( <button onClick={connect} disabled={wallet.connecting} className="btn btn-primary btn-sm"> {wallet.connecting ? ( <div className="flex items-center space-x-2"> <div className="loading-spinner w-3 h-3"></div> <span>连接中...</span> </div> ) : ( "连接钱包" )} </button> )} </div> </div> </div> ); };
